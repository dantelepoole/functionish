const expect = require('chai').expect;
const typeorclass = require('../typeorclass');

describe(`typeorclass()`, function() {

    beforeEach(
        function() {

        }
    )

    it(`should be tested`);
});
const iterable = require('../iterable');
const expect = require('chai').expect;

describe('iterable()', function() {

    beforeEach(
        function() {
        }
    )

    it('should be tested')

})
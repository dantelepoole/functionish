const mapiterable = require('../mapiterable');
const expect = require('chai').expect;

describe('mapiterable()', function() {

    beforeEach(
        function() {
        }
    )

    it('should be tested')
})
const expect = require('chai').expect;
const haslength = require('../haslength');

describe(`haslength()`, function() {

    beforeEach(
        function() {

        }
    )

    it(`should be tested`);
})

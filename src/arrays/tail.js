/**
 * @module arrays/tail
 */

'use strict';

/**
 * [to do]
 * 
 * @example
 * 
 * [to do]
 * 
 * @function tail
 * @param {any[]} array The array to retrieve the tail from
 * @returns {any}
 */
function tail(array) {
    return array[ array.length - 1 ];
}

module.exports = tail;

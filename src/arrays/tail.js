/**
 * @module collections/last
 */

'use strict';

/**
 * [to do]
 * 
 * @example
 * 
 * [to do]
 * 
 * @function tail
 * @param {any[]} array The array to retrieve the tail from
 * @returns {any}
 */
function tail(array) {
    return array.slice(1);
}

module.exports = tail;

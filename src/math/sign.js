/**
 * @module math/sign
 */

'use strict';

/**
 * Return 1 or -1, indicating the sign of *number*.
 * This function is merely a proxy for the {@link external:Math.sign Math.sign()} method.
 * 
 * @function sign
 * @see {@link external:Math.sign Math.sign()}
 * @param {number} number The value to return the sign of
 * @returns {number} 1 or -1
 */
module.exports = Math.sign;
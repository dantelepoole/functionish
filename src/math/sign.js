/**
 * @module math/sign
 */

'use strict';

/**
 * to do
 * 
 * @example
 * to do
 * 
 * @function sign
 * @param {number} value The value to retrieve the sign of
 * @returns {boolean}
 */
module.exports = Math.sign;
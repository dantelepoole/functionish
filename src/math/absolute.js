/**
 * @module math/absolute
 */

'use strict';

/**
 * Return the absolute value of *number*, i.e. always positive. This function is merely an alias for the
 * {@link external:Math.abs Math.abs()} method.
 * 
 * @function absolute
 * @see {@link external:Math.abs Math.abs()}
 * @param {number} value The value to return the absolute of
 * @returns {number} *number* as a positive value.
 */
module.exports = Math.abs;
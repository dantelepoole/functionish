/**
 * @module promises/pid
 */

'use strict';

/**
 * [to do]
 * 
 * @example <caption>Example usage of `pid()`</caption>
 * 
 * [to do]
 * 
 */
function pid(x) {
    return Promise.resolve(x);
}

module.exports = pid;
/**
 * @module misc/lowercase
 */

'use strict';

/**
 * to do
 * 
 * @example <caption>Example usage of `lowercase()`</caption>
 * 
 * to do
 * 
 * @function lowercase
 * @param {string} string The value to convert to lowercase
 * @returns {string}
 */
function lowercase(string) {
    return string.toLowerCase();
}

module.exports = lowercase;
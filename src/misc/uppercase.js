/**
 * @module misc/uppercase
 */

'use strict';

/**
 * to do
 * 
 * @example <caption>Example usage of `uppercase()`</caption>
 * 
 * to do
 * 
 * @function uppercase
 * @param {string} string The value to convert to uppercase
 * @returns {string}
 */
function uppercase(string) {
    return string.toUpperCase();
}

module.exports = uppercase;
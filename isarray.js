/**
 * @module isarray
 * @ignore
 */
'use strict';

/**
 * Alias for {@link external:Array.isArray Array.isArray()}.
 * 
 * @func isarray
 * @param {any} value The value to check
 * @returns {boolean}
 */
module.exports = Array.isArray;
'use strict';

const format = require('util').format;

/**
 * Alias for the `format()` method of Node's `util` package.
 * 
 * @module format
 * @see {@link external:util.format require('util').format()}
 */
module.exports = format;